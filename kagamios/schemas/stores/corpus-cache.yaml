type: corpus-cache
schema_version: 1
fields:
  id:
    type: string
    profile: minimal
    author: ai
  bibliographic_identity:
    type: ref
    profile: minimal
    author: ai
  contribution_line:
    type: text
    profile: minimal
    author: ai
    in_summary: true
  method_class:
    type: string
    profile: minimal
    author: ai
  evidence_type:
    type: string
    profile: minimal
    author: ai
  key_claims:
    type: list[object]
    profile: full
    author: ai
  content_source:
    # FR-54: which rung of the AD-29 sensor cost ladder produced the
    # content fields above — "abstract" when extraction ran, "none" when
    # the provider carried no abstract to extract from. An exposed
    # provider bias, never a silently-fabricated summary.
    type: string
    profile: minimal
    author: ai

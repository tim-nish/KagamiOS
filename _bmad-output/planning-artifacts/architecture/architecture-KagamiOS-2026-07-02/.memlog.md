---
scope: KagamiOS v1 MVP — states Frame..Locate, terminal deliverable accepted Gap Register; Propose/Decide deferred but must not be precluded
purpose: build-substrate for downstream epics/stories
altitude: feature
updated: 2026-07-03T19:53
---

- (event) Activation: Coaching path chosen; spine is the only deliverable (no deck/solution-design rendering). Inputs: approved PRD (prd-KagamiOS-2026-07-02, status final, Gap-Register-only MVP scope) + docs-spec/ as normative design source.
- (decision) AD candidate — Runtime shape: standalone deterministic CLI kernel that calls LLM APIs/an agent layer for generative work (option B). Binds: whole system. Prevents: enforcement-by-prompt-convention — write-guards, ASK scheduling, staleness traversal, derived-state computation, and event logging must be code, not convention (FR-21/29/31/37, E8). User-stated rationale: enforcement is the point. Claude Code integration allowed later only as an adapter, never as the kernel. [ADOPTED]
- (decision) SUPERSEDES prior runtime decision: user corrected a product-level assumption — KagamiOS is primarily a Claude Code plugin, sharing the BMAD ecosystem (co-installable). Standalone CLI as primary runtime rejected as product-direction mismatch. Deterministic mechanisms remain code, but hosted inside the plugin architecture.
- (constraint) Product constraint (user, load-bearing, NOT currently in the PRD): KagamiOS must install and run in the same ecosystem as BMAD — Claude Code plugin with skills + scripts, BMAD-style layout. Flag for PRD update: this platform requirement should be added to the PRD so upstream and architecture don't diverge.
- (decision) AD candidate ADOPTED — Runtime: Claude Code plugin with a deterministic core ('kernel as gatekeeper, not driver'). Components: (1) kagami deterministic scripts as the ONLY sanctioned mutation path (validation, versioning, staleness, derived state, generation windows, ASK batching, event log); (2) PreToolUse hooks deny direct AI Write/Edit to the artifact store; (3) roles = subagent definitions with restricted tools + script-rendered read-set-compliant context briefs; (4) the orchestrating skill IS the Interviewer role. Out-of-band (editor) file changes are definitionally human edits, reconciled by hash scan. User accepted two named v1 downgrades: incomplete main-thread token/prompt accounting, and frontier obedience being detect-and-audit rather than prevent — on the condition that illegal state mutation stays mechanically impossible and all deviations are auditable. Deterministic core must remain standalone-capable as a library (future CLI/adapter not precluded).
- (decision) AD ADOPTED — Core language: Python via uv, inheriting the BMAD ecosystem toolchain (co-installation implies shared runtime). Prevents: a second toolchain in the same plugin ecosystem.
- (decision) AD ADOPTED — Artifact store layout: explicit version files (option B). Each artifact = a directory with immutable v1.md, v2.md, ... plus meta.yaml holding current pointer, status, dependency pins. Version numbers are plain monotonic integers owned by the chokepoint script; staleness = integer comparison; immutability = vN.md never rewritten; git-friendly but git-independent. Prevents: version semantics depending on git state; history reconstruction from diffs.
- (decision) AD ADOPTED — Corpus tier: provider-abstraction REQUIRED. Multiple literature providers behind one common interface; OpenAlex and Semantic Scholar both supported as peers; default provider is CONFIGURATION, never hard-coded in the architecture (user rationale: availability/quotas/API policies drift). arXiv + GitHub as complementary sources. Paper cards as Markdown-with-frontmatter in a cross-run corpus store under chokepoint discipline. Prevents: single-provider coupling; provider choice baked into call sites.
- (decision) DEFERRED — Local embedding index over paper cards: v1 ships metadata + citation-graph + provider-side relevance search only. Revisit trigger: Scout's search demonstrably misses relevant clusters during dogfooding. (Embeddings remain sanctioned in the corpus tier per spec; the artifact tier stays graph-only permanently per Standing Refusal #5.)
- (decision) Batch of five structural calls ACCEPTED as written: (1) elicitation via kagami ask emit/answer + AskUserQuestion, E6 ask-before-show enforced by store refusal; (2) Deepen parallelism via one subagent per cluster with store-enforced section claims, sequential degraded mode valid; (3) event log JSONL appended only by scripts, role subagents on Claude Code models via Agent tool, model tiering as per-agent config; (4) plugin layout mirrors BMAD (skill + agents + hooks + python core + YAML schema registry), user data under configurable output root never in plugin dir; (5) single config.yaml for provider default, model tiers, output roots, privacy flag (default off).
- (event) Coaching complete; entering Finalize: distill spine, reconcile inputs, reviewer gate, triage, close.
- (version) Web-verified 2026-07-02: Python 3.14.x current stable (3.14.6, Jun 2026); pydantic 2.13.x current; Semantic Scholar API free (5k req/5min keyless, higher with free key), OpenAlex API moved to usage-based pricing Feb 2026 (free tier remains) — the OpenAlex pricing change empirically validates the provider-abstraction AD.
- (event) Reviewer gate — versions lens complete (reviews/review-versions.md): all load-bearing platform claims verified (plugins ship skills+agents+hooks; PreToolUse can deny; per-agent model frontmatter exists). Corrections to apply: S2 rate-limit row wrong (keyless = shared 1000 RPS global pool, free key = 1 RPS dedicated); OpenAlex now requires free API key (/day allowance); GitHub search needs token (~30 req/min); PyYAML can't round-trip comments — declare meta.yaml machine-owned/comment-free and core never rewrites config.yaml; AD-12 needs dispatch-time model resolution note (subagent model frontmatter is static; plugin subagents ignore hooks/mcpServers frontmatter).
- (event) Reviewer gate — adversarial lens complete (reviews/review-adversarial.md): 9 holes. Most dangerous: (1) concurrent kagami CLI processes unserialized — claim checks/version minting/meta.yaml/events.jsonl race under parallel Deepen; needs run-level lock AD. (2) AD-2 denies Write/Edit/redirect but cp/sed -i writes get stamped human by AD-3 — provenance forgery; hook coverage must be default-deny on the output root. (3) 'human edits always win over queued AI writes' encoded nowhere — queued regen can supersede an unscanned human edit; scan-before-write rule needed. (4) ledger.md prose format clashes with versioned ledger-entry semantics (05_elicitation section 5) — ledger needs a registry-governed machine format. (5) cross-run stores lack versioning/locking rules; researcher-profile ambiguously both artifact type and cross-run path. (6) section-ID minting/survival semantics unspecified — renames orphan claims/deps. Plus: AD-4 role-session citation and AD-11 no-readback flagged unenforceable as written; meta.yaml ownership; manifest-vs-kernel dual state authority.
- (event) Reviewer gate — input reconciliation complete (reviews/reconcile-inputs.md): 3 contradictions, 9 dropped-with-divergence-risk, 9 harmless, 3 scope-creep. Top: C1 embedding deferral contradicts docs-spec 07 corpus-tier hybrid retrieval spec (needs user decision or reframe as approved deviation — user explicitly chose the deferral in coaching); D1 tiered repair pipeline (FR-35) missing entirely from ADs/Deferred despite MVP-hot; D8 FR-47 missing from binds + 08 sect4's two locked-from-run-1 schema commitments (question-class key, late outcome joins) have no AD-14-style non-preclusion analog. Also D2 summary-regen-at-acceptance, D3 human-edits-win (matches adversarial hole 3), D4 per-cluster window keying, D5 manifest monitoring config. Platform NFR negotiated conditions landed intact.
- (event) Reviewer gate — rubric walker complete (reviews/review-rubric.md): revise-before-build; 1 critical (AD-2/AD-3 provenance laundering via hook evasion), 5 high (vN.md-vs-human-edit mechanism, concurrency/crash/claims, install+migration silent, no core-to-model protocol, Dormant monitoring homeless), 8 medium, 4 low.
- (change) Spine revised 2026-07-03 integrating all four reviews; lint clean. Amended in place: AD-2 (default-deny allowlist, never blocklist), AD-3 (data-plane vs control-plane; turn-aware attribution; suspect quarantine; git/formatter edge acknowledged), AD-4 (worker role added; briefs keyed by role,state,target with 07-sect3 read-set table normative; hook-registered sessions + single-use engagement tokens; residual off-charter risk moved to honest-gap), AD-6 (current.md sole human edit surface; summary-at-acceptance as validated input), AD-9 (per-cluster window keying, Candidate window run-level; impossibility claim scoped to creation, display = detect-and-audit), AD-10 (lease-bound claims), AD-11 (llm_call carries operation_class/model_tier/tokens/cache-hit; FR-5 exception carried; honest-gap register consolidated), AD-12 (per-operation-class dispatch table in schemas/dispatch.yaml; launch-time model resolution; credentials env-only), AD-14 (infrastructure-store schemas; run-1 locks: question-class key, outcome joins, audit_exempt flag for FR-47).
- (change) New ADs 15-25: AD-15 run lock + single-writer lease + fixed write order + torn-op repair; AD-16 scan-before-write, human edits always win, overlap -> proposed diff; AD-17 ledger = schema-governed record store (q-NNN.yaml, versioned entries, consumed_by in entry meta), ledger.md a render; AD-18 cross-run contracts (profile = AD-6 artifact; corpus/registry derived+unversioned with content-derived IDs + schema_version + per-store locks); AD-19 opaque registered section IDs, explicit create/retire; AD-20 manifest = cache + non-derivable facts only, guards call the pure function; AD-21 tiered repair (frontier-triggered, sectional, proposed-diff on human spans); AD-22 needs_llm protocol (validated input arg or typed work item, no third pattern); AD-23 install (pyproject + uv run --project CLAUDE_PLUGIN_ROOT) + migration (schema_registry_version stamp, refuse-newer, kagami migrate); AD-24 monitoring = sweep at run open, Dormant reopens next session (explicit FR-7 narrowing, PRD wording amendment recommended); AD-25 testing at CLI boundary with scripted driver + golden minimal-run fixture + events-deletion invariance.
- (decision) Embedding deferral reframed per reconcile C1 + rubric M4: now a RECORDED DEVIATION from docs-spec 07 sect5-6 (researcher-decided), with the interim partition mechanism fixed (citation-graph communities + provider-side relevance; FR-26 two cuts derivable from that pair) and a dogfooding revisit trigger. Stack corrected per versions lens: S2 rate limits, OpenAlex mandatory key, GitHub token, arXiv backoff, PyYAML machine-owned-YAML convention. binds line split into binds / non-preclusion / deferred (L1).
- (event) spine finalized (status: final, 2026-07-03). 25 ADs, lint clean, all four reviewer lenses applied. Open recommendation for researcher: one-line PRD wording amendment to FR-7 ('reopened automatically' -> 'reopened at next session'), per AD-24's sweep-not-daemon narrowing.
- (decision) Update triggered by change-signal-epic7-2026-07-03.md: Epics 1-6 shipped the deterministic core (337 tests passing) but no epic covers the harness/driver the spine already specifies (skills/, agents/, schemas/dispatch.yaml). PRD already amended with FR-48 (refusal-retry ceiling), FR-49 (llm_call reporting entrypoint), FR-37 budget-warning consequence. This Update adds the architecture-side ADs Epic 7 needs to be story-ready.
- (decision) AD-4 amended: kagami session open/close and single-use engagement tokens are DEFERRED for v1-driver (Epic 7 walking skeleton does not implement them). Interim: role attribution comes from the harness self-reporting role on each llm_call via FR-49's reporting entrypoint (AD-26), trusted rather than mechanically gated by a token check. This is a real weakening of AD-4's original token-gated write path and is recorded as a new AD-11 honest-gap entry, not silently accepted. Revisit trigger: if Epic 7 dogfooding (7.5) surfaces a role writing off-charter content that a token check would have caught, promote session/token implementation from deferred to required before further driver work.
- (decision) New AD-26 ADOPTED — Driver operational discipline. Binds FR-48/FR-49/FR-37's new consequence. Rule: (a) chokepoint tracks consecutive identical refusals per (entrypoint, target) under the run lock (AD-15); attempt N+1 after a configured ceiling returns requires_researcher instead of an ordinary refusal (FR-48) -- prevents unbounded retry-storm token burn, the failure mode identified during pre-implementation review as the main runaway-cost risk since the core itself cannot loop, only the harness around it can. (b) the harness calls a new validating kagami entrypoint immediately after every model invocation, supplying role/operation_class/model_tier/tokens/cache-hit (FR-49) -- this is AD-22's needs_llm protocol in reverse: harness fulfills work AND reports it, both through validating entrypoints, never a bare append. (c) kagami metrics's decision block (FR-37) compares the run's cumulative token ledger against a config.yaml soft limit and adds a warning field, never a block -- preserves the NFR5 cost-discipline-via-retrieval-boundaries-not-budgets commitment; this is reporting at an existing checkpoint, not new live enforcement.
- (decision) New AD-27 ADOPTED -- Testing convention for prompt artifacts. Binds skills/, agents/ (SKILL.md, agent definitions). Prevents Epic 7 stories from getting unfalsifiable acceptance criteria, and prevents AD-25's CLI-boundary proof from being mistaken for covering the driver too -- AD-25 verifies kagami/ only; skills/ and agents/ are prompts, not code, and cannot be pytest-verified. Rule: a driver story's Definition of Done requires (a) a recorded-transcript check -- a real Claude Code session transcript demonstrating the story's acceptance criteria end-to-end, attached to the PR (extends the implement-issue convention already in use); (b) a checklist review against the role/skill's charter -- does the artifact violate any AD, does any FR consequence fail on inspection; (c) for the walking-skeleton and toy-run stories specifically, a fixed repeatable golden toy-run protocol (topic, corpus scope, config pinned) whose event log is inspected against the evaluation rubric fixed at Epic-7 story-design time. None of these substitute for AD-25's suite on the core -- they are the driver's parallel, weaker-but-honest verification path, itself logged as a new AD-11 honest-gap entry.
- (event) Reviewer gate run against the AD-26/AD-27 delta: versions lens = pass with minor fixes (AD-25 hooks/ scope citation, NFR5 fragile-number reference, AD-26/AD-11 citation swap); adversarial lens = needs-fixes, 6 holes (refusal-counter storage/identity/reset undefined, llm_call reporting entrypoint unnamed with no idempotency guard, AD-4 content-write vs AD-26 telemetry role attribution had no join key, AD-11 one-sanctioned-read-exception text contradicted by AD-26c's unconditional gate-time read, AD-1 left unamended against AD-26b's honor-system reporting obligation, requires_researcher unreconciled with AD-8/AD-14 ledger machinery). All 6 adversarial holes and all 3 versions fixes applied directly: AD-26a now derives the refusal counter from events.jsonl (AD-20-style, no new store), defines identical=(entrypoint,target) tuple with reset-on-non-refusal, and states requires_researcher is a synchronous CLI response outside kagami ask/AD-14; AD-26b names the entrypoint kagami report llm-call with a harness-minted --call-id idempotency guard; AD-4's amendment collapses content-write and telemetry role attribution into one self-declared --role, no separate join key; AD-11 now distinguishes behavior-altering reads (FR-5, the one exception) from kagami metrics' routine reporting reads (always allowed, AD-26c is this category) and cites AD-4/AD-26 correctly; AD-1 gained an amendment note distinguishing enforceable guarantees from the reporting obligation, filed under the existing honest-gap category; AD-25 citations in AD-11 and AD-27 corrected to include hooks/. Lint clean after each pass (0 findings).
- (event) spine re-finalized for Epic 7 (status: final, updated: 2026-07-03). AD count 25 -> 27. AD-1, AD-4, AD-11 amended in place (IDs stable); AD-26, AD-27 appended. Frontmatter binds extended with FR-48, FR-49. Capability map gained a row for section 4.8. Next: bmad-create-epics-and-stories, append Epic 7 using the same change signal, PRD, and this spine as inputs.
